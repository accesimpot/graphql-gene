query PageUnionBlocks($path: String!) {
  pageByPath(where: { path: { eq: $path } }) {
    id
    path
    blocks {
      id

      content {
        __typename
        ... on HeroBlock {
          title
          subtitle
        }
        ... on TextBlock {
          body
        }
      }
    }
  }
}
